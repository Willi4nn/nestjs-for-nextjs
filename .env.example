# Blog API (.env example)
# ==========================
# Renomeie para ".env" para uso local.

# Banco de Dados - PostgreSQL
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=seu_usuario
DB_PASSWORD=sua_senha
DB_DATABASE=seu_banco

# Em produção, DB_SYNCHRONIZE deve ser "0"
DB_SYNCHRONIZE=1
DB_AUTO_LOAD_ENTITIES=1

# JWT (Autenticação)
# ==========================
# Defina JWT_SECRET em produção com valor seguro.
JWT_SECRET=
JWT_EXPIRATION=1d

# Porta da aplicação
APP_PORT=3000

# Ambiente: production, development, etc
NODE_ENV=development

# CORS: domínios permitidos (separados por espaço)
CORS_WHITELIST="http://localhost:3000"